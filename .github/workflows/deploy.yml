name: Deploy Hearth to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'content/**'
      - 'hearth/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install image tools
        run: sudo apt-get update && sudo apt-get install -y webp

      - name: Stage Hearth files
        run: |
          cp -r content _site_hearth
          cp hearth/index.html _site_hearth/index.html

          # Generate WebP for all PNGs
          find _site_hearth -name '*.png' | while IFS= read -r f; do
            cwebp -q 85 "$f" -o "${f%.png}.webp"
          done

          # Wrap under hearth/v1/
          mkdir -p _site/hearth
          mv _site_hearth/index.html _site/index.html
          mv _site_hearth _site/hearth/v1

      - name: Generate manifest
        run: |
          cd _site/hearth
          echo '{' > manifest.json
          echo '  "version": "1",' >> manifest.json
          echo '  "generated": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'",' >> manifest.json
          echo '  "files": {' >> manifest.json

          find v1 -type f | sort | while IFS= read -r f; do
            hash=$(sha256sum "$f" | cut -c1-12)
            size=$(stat -c%s "$f")
            echo "    \"$f\": {\"sha256\": \"$hash\", \"size\": $size},"
          done | sed '$ s/,$//' >> manifest.json

          echo '  }' >> manifest.json
          echo '}' >> manifest.json

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
