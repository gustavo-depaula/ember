import { Stack } from 'expo-router'

// Native stack so the search screen can host the iOS 26 header search bar that
// morphs out of the bottom search tab.
export default function SearchStackLayout() {
  return <Stack />
}
