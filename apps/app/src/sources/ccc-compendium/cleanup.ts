// The entity decoder + chrome stripper are shared with the full-Catechism
// scraper; the implementation lives in ../vatican/cleanup.
export { cleanChapter, decodeEntities } from '../vatican/cleanup'
